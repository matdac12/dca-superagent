# Example Custom Command

This is an example slash command. You can invoke it by typing `/example` in Claude Code.

Replace this content with your custom command instructions. For example:

- Review the code for security vulnerabilities
- Run the test suite and fix any failures
- Generate documentation for the current module
